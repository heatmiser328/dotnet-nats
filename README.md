# dotnet-nats
NATS client for .NET



Lots to do...

### Developing

1. Clone the repo
2. dotnet-sockets is a submodule, so execute the following to retrieve it

```
git submodule init
git submodule update
```

